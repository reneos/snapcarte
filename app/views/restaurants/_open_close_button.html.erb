<% if policy(restaurant).update? %>
<% btn_text = restaurant.open ? "Close restaurant" : "Open restaurant" %>
<%= link_to btn_text, restaurant_path(restaurant), class:"btn btn-primary", method: :patch, remote: true %>
<% end %>
